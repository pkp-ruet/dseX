/**
 * TopStock AI — the assistant's persona and single source of truth for its
 * name. Change it here and it updates everywhere: the floating button, panel
 * header, avatar, greeting, and the /assistant page heading.
 */
export const PERSONA = {
  name: "TopStock AI",
  /** Letters shown in the avatar circle. */
  initial: "TS",
  tagline: "Your DSE stock helper",
} as const;
