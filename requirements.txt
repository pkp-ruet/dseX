requests>=2.31.0
beautifulsoup4>=4.12.0
pymongo>=4.6.0
python-dotenv>=1.0.0
schedule>=1.2.0
lxml>=5.1.0
pandas>=2.1.0
numpy>=1.26.0
# Needed by `python main.py notify-digest` (run from CI): the push digest reuses
# campaign_service, which imports email_service (python-jose), and sends via pywebpush.
python-jose[cryptography]>=3.3.0
pywebpush>=1.14.0
# Bengali "এক নজরে" stock summaries default to a free, offline template renderer
# (no AI). The Anthropic SDK is only needed if you opt into SUMMARY_BACKEND=ai —
# uncomment then. The FastAPI read path never imports it.
# anthropic>=0.49.0
